package it.petrovich.rss.common;

public enum RegistrationStatus {
    /**
     * Subscription saved successfully.
     * Uses in the response.
     */
    SUCCESS,
    /**
     * Subscription does not save.
     * Uses in the response.
     */
    FAIL
}
